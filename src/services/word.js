const Word      =   require('../models/Word');
const _promise  =   require('../helpers/simpleAsync');

module.exports.createWord = async wordInfo => {
	return new Promise(async (resolve, reject) => {

	});
};